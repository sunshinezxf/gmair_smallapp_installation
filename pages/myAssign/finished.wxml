<view class="center">
  <view wx:for="{{assign_list}}" wx:key="{{item.assignId}}">
    <view class="flex-cell flex-row">
      <view class='flex-cell flex-row'>{{item.codeValue}}</view>
      <view class='flex-cell flex-row'>{{item.createAt}}</view>
    </view>
  </view>
</view>


